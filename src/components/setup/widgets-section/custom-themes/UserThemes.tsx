import React from 'react';

interface UserThemesProps {
  onLoaded?: () => void;
}

const UserThemes: React.FC<UserThemesProps> = () => {
  return <div>UserThemes</div>;
};

export default UserThemes;
